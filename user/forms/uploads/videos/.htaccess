# Prevent direct access to uploaded video files
# Files can only be accessed through download.php

# Deny all direct access
Order Allow,Deny
Deny from all

# Prevent directory listing
Options -Indexes

# Prevent execution of PHP files
php_flag engine off
